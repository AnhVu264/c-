#include <iostream>

using namespace std;

int main()
{
    int a = 1, n = -1;
    while (a > 0)
    {
        cin >> a;
        if (a != n)
        {
            cout << a << " ";
        }
        n = a;
    }


    return 0;
}
